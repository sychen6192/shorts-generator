"""shortsloop — unattended nightly Wan 2.2 short-video pipeline.

generate → judge → re-roll failures → encode survivors → morning report.
Frozen contracts live in docs/plan.md §2; hard rules in CLAUDE.md.
"""

__version__ = "0.1.0"
SCHEMA_VERSION = "1"
